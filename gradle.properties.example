# Copy to ~/.gradle/gradle.properties for publishing and consuming GitHub Packages.
# Create a PAT: GitHub → Settings → Developer settings → Personal access tokens
# Scopes: read:packages (consume), write:packages + repo (publish)

gpr.user=YOUR_GITHUB_USERNAME
gpr.key=ghp_xxxxxxxxxxxxxxxxxxxx
